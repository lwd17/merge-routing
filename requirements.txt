# Core runtime
openai>=1.30
json-repair>=0.25
httpx>=0.27
rich>=13.0
sentence-transformers>=2.6
datasets>=2.19

# Retrieval (requires Java 21 on PATH for Lucene indexing/search)
pyserini>=0.36

# Learned local value (training + numpy inference)
numpy>=1.26
torch>=2.2
